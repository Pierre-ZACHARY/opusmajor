apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: player-data

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml

labels:
  - pairs:
      app.kubernetes.io/name: player-data
      app.kubernetes.io/part-of: opusmajor
